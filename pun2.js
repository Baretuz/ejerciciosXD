const num1 = parseFloat(prompt("ingrese el primer numero:"));
const num2 = parseFloat(prompt("ingrese el segundo numero:"));

const cuadradonumero1 = (num1 * num1);
const cuadradonumero2 = (num2 * num2);

console.log (`el cuadrado del primer numero = (${num1}) es: ${cuadradonumero1}`);

console.log (`el cuadrado del primer numero = (${num2}) es: ${cuadradonumero2}`);